import SignUpPage from '@/screens/SignUpPage';

/*
  This component serves as the entry point for the sign-up page.
  It renders the SignUpPage component, which provides the user interface for creating a new account.
*/
export default function SignIn() {
  return (
    <SignUpPage />
  );
}