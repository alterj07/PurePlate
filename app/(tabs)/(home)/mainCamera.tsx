/*
  This file serves as the entry point for the main camera page.
  It imports and renders the CameraScreen component, which provides the user interface for capturing photos.
*/
import mainCamera from '@/screens/CameraScreen';

export default mainCamera;