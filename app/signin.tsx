import SignInPage from '@/screens/SignInPage';

/*
  This component serves as the entry point for the sign-in page.
  It renders the SignInPage component, which provides the user interface for logging into an account.
*/
export default function SignIn() {
    return (
        <SignInPage />
    );
}